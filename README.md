Testing email authentication in Streamlit
Using hashed one time code (OTC) method

Hold list of authenticated users in mysql table, validate against table
send otc to email, validate against hash in users table (mysql)
pass / fail to dummy homepage
